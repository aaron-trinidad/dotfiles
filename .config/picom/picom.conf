################################
# Backend
################################

backend = "glx";
vsync = true;

################################
# Transparencia
################################

detect-client-opacity = true;

################################
# Blur
################################

blur-method = "dual_kawase";
blur-strength = 2;

blur-background = true;

blur-background-exclude = [
    "class_g = 'Polybar'",
    "class_g = 'Rofi'"
];
